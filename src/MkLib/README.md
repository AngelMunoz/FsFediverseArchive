# MkLib

This is a sample of a shared library that doesn't share any logic besides types and JSON translation functions, the idea behind serverless AFAIK is to have self contained lightweight functions but in cases like this (a sample) it should be good enough to share some types and logic
